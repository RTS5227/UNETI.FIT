@model UNETI.FIT.Models.ViewModels.ListViewModelBase<UNETI.FIT.Models.Entity.Category>
@{
    ViewBag.Title = Resources.Resource.ListCategoryManager;
}

@section navbar{
    <li class="active">@ViewBag.Titler</li>
}

<div class="box box-primary">
    <div class="box-header">
        <p class="btn btn-primary">
            @Html.ActionLink(Resources.Resource.Create, "create")
        </p>
        <p class="btn btn-primary">
            @Html.ActionLink(Resources.Resource.ExportToExcel, "export")
        </p>
    </div>
    <div class="box-body">
        <!-- table -->
        @{
            Html.RenderPartial("_TableCategory", Model.List);
        }
    </div>
    <div class="box-footer">
        <!--Phan Trang-->
        @{
            Html.RenderPartial("_Pagination", Model.Filter);
        }
    </div>
</div>

