@using UNETI.FIT.Models.Entity
@model Category

@{
    ViewBag.Title = Resources.Resource.Create;
}

@section navbar{
    <li>@Html.ActionLink(Resources.Resource.ListCategoryManager, "index")</li>
    <li class="active">@ViewBag.Title</li>
}
<div class="box box-primary">
    <div class="box-header">
        <h2 class="box-title">@ViewBag.Title</h2>
    </div>
    @using (Html.BeginForm("add", "Category", FormMethod.Post, new { enctype = "multipart/form-data" }))
    {
        @Html.ValidationSummary(true)

        <fieldset>
            <legend></legend>

            <div class="box-body">
                @Html.EditorForModel()
            </div>

            <p class="box-footer">
                <input type="submit" class="btn btn-primary" value="@Resources.Resource.Create" />
            </p>
        </fieldset>
    }
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
