@model UNETI.FIT.Areas.SubjectManager.Models.Entity.Teacher

@{
    ViewBag.Title = Resources.Resource.Edit;
}

@section navbar{
    <li>@Html.ActionLink(Resources.Resource.ListTeacherManager, "index")</li>
    <li class="active">@ViewBag.Title</li>
}

<div class="box box-primary">
    <div class="box-header">
        <h2 class="box-title">@Model.FullName</h2>
    </div>
    @using (Html.BeginForm("edit", "Teacher", FormMethod.Post, new { enctype = "multipart/form-data" }))
    {
        @Html.ValidationSummary(true)

        <fieldset>
            <legend></legend>

            <div class="box-body">
                @Html.EditorForModel()
            </div>

            <p class="box-footer">
                <input type="submit" class="btn btn-primary" value="@Resources.Resource.Save" />
            </p>
        </fieldset>
    }
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
