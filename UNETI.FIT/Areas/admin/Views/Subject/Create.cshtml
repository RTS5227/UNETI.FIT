@model UNETI.FIT.Areas.SubjectManager.Models.Entity.Subject

@{
    ViewBag.Title = Resources.Resource.Create;
}

@section navbar{
    <li>@Html.ActionLink(Resources.Resource.ListSubjectManager, "index")</li>
    <li class="active">@ViewBag.Title</li>
}
<div class="box box-primary">
    <div class="box-header">
        <h2 class="box-title"></h2>
    </div>
    @using (Html.BeginForm("create", "Subject", FormMethod.Post, new { enctype = "multipart/form-data" }))
    {
        @Html.ValidationSummary(true)

        <fieldset>
            <legend></legend>

            <div class="box-body">
                @Html.EditorForModel()
            </div>

            <p class="box-footer">
                <input type="submit" class="btn btn-primary" value="@Resources.Resource.Save" />
            </p>
        </fieldset>
    }

</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
